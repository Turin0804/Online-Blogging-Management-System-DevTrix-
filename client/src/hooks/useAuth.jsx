// Stub useAuth hook — replace with real AuthContext when ready
const useAuth = () => {
    return {
        user: null,
        logOut: () => {},
        loading: false,
    };
};

export default useAuth;
